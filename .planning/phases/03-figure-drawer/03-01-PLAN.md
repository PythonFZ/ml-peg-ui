---
phase: 03-figure-drawer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/storage.py
  - api/models.py
  - api/index.py
  - tests/test_api.py
autonomous: true
requirements: [FR-3.1, FR-3.4]

must_haves:
  truths:
    - "GET /api/v1/benchmarks/{slug}/figures returns list of figure slugs for that benchmark"
    - "GET /api/v1/benchmarks/{slug}/figures/{figure_slug} returns figure JSON data"
    - "Figure endpoint returns 307 redirect with presigned URL when file exceeds 4 MB"
    - "Benchmarks without figures return empty array from figures index"
    - "Unknown benchmark slug returns 404"
  artifacts:
    - path: "api/storage.py"
      provides: "get_object_size() method on both backends"
      contains: "get_object_size"
    - path: "api/models.py"
      provides: "FigureItem, FigureListResponse, FigureResponse Pydantic models"
      contains: "FigureListResponse"
    - path: "api/index.py"
      provides: "Two figure endpoints replacing 501 stub"
      contains: "benchmark_figures_index"
    - path: "tests/test_api.py"
      provides: "Tests for figure index, figure detail, 307 redirect, 404 cases"
      contains: "test_benchmark_figures_index"
  key_links:
    - from: "api/index.py"
      to: "api/storage.py"
      via: "storage.get_json() and storage.get_object_size()"
      pattern: "storage\\.get_object_size"
    - from: "api/index.py"
      to: "api/models.py"
      via: "FigureListResponse, FigureResponse imports"
      pattern: "FigureListResponse"
---

<objective>
Implement the backend figure-serving endpoints: a figures index endpoint that lists available figure slugs per benchmark, and a figure detail endpoint with size-based 307 presigned URL redirect for large files.

Purpose: The frontend needs endpoints to fetch figure data for the drawer. The index endpoint enables knowing which figures exist per benchmark (and whether a benchmark has any figures at all). The detail endpoint serves actual Plotly JSON.

Output: Two working API endpoints, storage `get_object_size()` method, Pydantic response models, and comprehensive tests.
</objective>

<execution_context>
@/Users/fzills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fzills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-figure-drawer/03-CONTEXT.md
@.planning/phases/03-figure-drawer/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From api/storage.py:
```python
@runtime_checkable
class StorageBackend(Protocol):
    def get_json(self, path: str) -> dict: ...
    def list_keys(self, prefix: str) -> list[str]: ...
    def presigned_url(self, path: str) -> str: ...

class FilesystemBackend:
    def __init__(self, base_path: str = "data") -> None: ...
    # _base = Path(base_path)

class MinioBackend:
    def __init__(self) -> None: ...
    # client = Minio(...), bucket, prefix
    def _key(self, path: str) -> str: ...
```

From api/models.py:
```python
class Meta(BaseModel):
    count: int
    columns: list[str] | None = None

class BenchmarkRow(BaseModel):
    model_config = ConfigDict(extra="allow")
    id: str; MLIP: str; Score: float | None = None
```

From api/index.py:
```python
CACHE_HEADER = "s-maxage=3600, stale-while-revalidate=86400"
# slug_map: dict[str, str] — maps lowercase slug -> "category/BenchmarkDir"
# Figure files: {bench_path}/figure_{figure_slug}.json
# Current stub at line 193-207 returns 501
```

Data layout:
```
data/{category}/{BenchmarkDir}/figure_{slug}.json
# Example: data/conformers/37Conf8/figure_37conf8.json
# Multi-figure: data/thermochemistry/GSCDB138_thermochemistry/figure_gscdb138_DC13.json
# Figure slug = filename minus "figure_" prefix and ".json" suffix
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add get_object_size to storage + Pydantic figure models</name>
  <files>api/storage.py, api/models.py</files>
  <behavior>
    - get_object_size("conformers/37Conf8/figure_37conf8.json") returns int > 0
    - get_object_size("nonexistent/file.json") raises FileNotFoundError
    - FigureItem model validates {"slug": "37conf8", "name": "37conf8"}
    - FigureListResponse validates {"data": [...], "meta": {"count": 1}}
  </behavior>
  <action>
    1. In `api/storage.py`:
       - Add `get_object_size(self, path: str) -> int` to `StorageBackend` Protocol
       - Implement on `FilesystemBackend`: `(self._base / path).stat().st_size`, raise `FileNotFoundError` if not exists
       - Implement on `MinioBackend`: `self.client.stat_object(self.bucket, self._key(path)).size`

    2. In `api/models.py`:
       - Add `FigureItem(BaseModel)` with fields: `slug: str`, `name: str`
       - Add `FigureListResponse(BaseModel)` with fields: `data: list[FigureItem]`, `meta: Meta`
       - Add `FigureResponse(BaseModel)` with fields: `data: dict` (raw Plotly JSON payload)
  </action>
  <verify>
    <automated>uv run pytest tests/test_api.py -x -q</automated>
  </verify>
  <done>StorageBackend protocol has get_object_size, both backends implement it, three new Pydantic models exist in api/models.py</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement figure index and detail endpoints with tests</name>
  <files>api/index.py, tests/test_api.py</files>
  <behavior>
    - GET /api/v1/benchmarks/37conf8/figures returns 200 with {"data": [{"slug": "37conf8", "name": "37conf8"}], "meta": {"count": 1}}
    - GET /api/v1/benchmarks/nonexistent/figures returns 404
    - GET /api/v1/benchmarks/37conf8/figures/37conf8 returns 200 with {"data": {plotly JSON}}
    - GET /api/v1/benchmarks/37conf8/figures/nonexistent returns 404
    - GET /api/v1/benchmarks/phonons/figures returns 200 with {"data": [], "meta": {"count": 0}} (benchmark without figures)
    - Existing test_figure_stub test must be updated (endpoint no longer returns 501)
  </behavior>
  <action>
    1. In `api/index.py`:
       - Add import for `FigureItem`, `FigureListResponse`, `FigureResponse` from `api.models`
       - Replace the 501 stub endpoint with two endpoints:

       **Figures index** (`GET /api/v1/benchmarks/{slug}/figures`):
       - Look up `bench_path` from `slug_map` (lowercase). Return 404 if not found.
       - Use `storage.list_keys(bench_path)` to get all files in the benchmark directory
       - Filter for files matching `figure_*.json` pattern
       - Extract figure slug from filename: strip `figure_` prefix and `.json` suffix
       - Return `FigureListResponse` with `FigureItem` list and `Meta(count=N)`
       - Set Cache-Control header

       **Figure detail** (`GET /api/v1/benchmarks/{slug}/figures/{figure_slug}`):
       - Look up `bench_path` from `slug_map`. Return 404 if not found.
       - Build file path: `{bench_path}/figure_{figure_slug}.json`
       - Call `storage.get_object_size(figure_file)` — catch `FileNotFoundError` -> 404
       - If size > 4 * 1024 * 1024 (4 MB): return `RedirectResponse(url=storage.presigned_url(figure_file), status_code=307)`
       - Otherwise: `payload = storage.get_json(figure_file)`, set Cache-Control, return `FigureResponse(data=payload)`
       - Import `RedirectResponse` from `fastapi.responses`

    2. In `tests/test_api.py`:
       - Import `FigureListResponse` from `api.models`
       - Replace `test_figure_stub` with these tests:
         - `test_benchmark_figures_index`: GET /api/v1/benchmarks/37conf8/figures returns 200, validates FigureListResponse, at least 1 figure, each has slug+name
         - `test_benchmark_figures_index_404`: GET /api/v1/benchmarks/nonexistent/figures returns 404
         - `test_benchmark_figure_json`: GET /api/v1/benchmarks/37conf8/figures/37conf8 returns 200, body has "data" key containing dict with "data" key (Plotly traces array)
         - `test_benchmark_figure_404`: GET /api/v1/benchmarks/37conf8/figures/nonexistent_figure returns 404
         - `test_benchmark_figures_empty`: Find a benchmark without figures (e.g. phonons or diatomics benchmark slug) and verify empty data array with count 0
         - `test_benchmark_figure_cache_headers`: Verify Cache-Control header on figure detail response
  </action>
  <verify>
    <automated>uv run pytest tests/test_api.py -x -q</automated>
  </verify>
  <done>
    - Figures index returns correct figure list for benchmarks with figures, empty list for benchmarks without
    - Figure detail returns Plotly JSON inline with Cache-Control headers
    - 307 redirect code path exists (not triggered for current data but present)
    - All new tests pass, existing tests still pass
    - Old 501 stub test is replaced
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_api.py -x -q` — all tests pass including new figure tests
- `uv run pytest -x -q` — full test suite green
- Manually verify: `GET /api/v1/benchmarks/37conf8/figures` returns figure list
- Manually verify: `GET /api/v1/benchmarks/37conf8/figures/37conf8` returns Plotly JSON
</verification>

<success_criteria>
- Two new endpoints serving figure data (index + detail)
- Size-based 307 redirect logic in place for FR-3.4
- Storage protocol extended with get_object_size
- All tests pass (old + new)
</success_criteria>

<output>
After completion, create `.planning/phases/03-figure-drawer/03-01-SUMMARY.md`
</output>
