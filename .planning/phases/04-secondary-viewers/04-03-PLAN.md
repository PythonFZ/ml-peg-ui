---
phase: 04-secondary-viewers
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/lib/types.ts
  - src/lib/api.ts
  - src/components/Mol3DViewer.tsx
  - src/components/StructureModal.tsx
  - src/components/FigureDrawer.tsx
autonomous: true
requirements:
  - FR-6.2

must_haves:
  truths:
    - "Benchmarks with xyz structure files show a 'View Structure' button in the figure drawer"
    - "Clicking 'View Structure' opens a modal with a 3D ball-and-stick molecular rendering"
    - "Crystal structures (pbc=T T T) display a unit cell wireframe box"
    - "3Dmol.js loads lazily — not included in initial page bundle"
  artifacts:
    - path: "src/components/Mol3DViewer.tsx"
      provides: "3Dmol.js wrapper component for WebGL molecular rendering"
      min_lines: 30
    - path: "src/components/StructureModal.tsx"
      provides: "MUI Dialog wrapping Mol3DViewer with loading state"
      min_lines: 25
    - path: "src/components/FigureDrawer.tsx"
      provides: "Updated drawer with 'View Structure' button for xyz-containing benchmarks"
      contains: "View Structure"
  key_links:
    - from: "src/components/FigureDrawer.tsx"
      to: "src/components/StructureModal.tsx"
      via: "View Structure button opens modal"
      pattern: "StructureModal"
    - from: "src/components/StructureModal.tsx"
      to: "src/components/Mol3DViewer.tsx"
      via: "dynamic import with ssr: false"
      pattern: "dynamic.*Mol3DViewer"
    - from: "src/components/StructureModal.tsx"
      to: "/api/v1/structures/{slug}/{model}/{file}"
      via: "useStructure SWR hook"
      pattern: "useStructure"
---

<objective>
Build the 3D structure viewer: a lazy-loaded 3Dmol.js component shown in a modal from the figure drawer, with ball-and-stick rendering and unit cell display for crystal structures.

Purpose: Researchers need to visualize atomic structures for benchmarks that have xyz files. The "View Structure" button in the existing figure drawer keeps the natural leaderboard -> figure -> structure flow.

Output: Mol3DViewer component, StructureModal, updated FigureDrawer with structure button, TS types, SWR hook.
</objective>

<execution_context>
@/Users/fzills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fzills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-secondary-viewers/04-CONTEXT.md
@.planning/phases/04-secondary-viewers/04-RESEARCH.md
@.planning/phases/04-secondary-viewers/04-01-SUMMARY.md

<interfaces>
<!-- Existing FigureDrawer interface -->

From src/components/FigureDrawer.tsx:
```typescript
interface FigureDrawerProps {
  open: boolean;
  onClose: () => void;
  benchmarkSlug: string | null;
  benchmarkName: string;
  filterModel: string | null;
}
// Uses useBenchmarkFigures(slug) to get figure list
// FigurePanel renders each figure with PlotlyChart
```

From src/lib/api.ts:
```typescript
export const fetcher = (url: string) => fetch(url).then((r) => r.json());
```

New types from Plan 01 backend (api/models.py):
```python
# StructureResponse: { data: { xyz_string: str, has_pbc: bool } }
```

3Dmol.js API (from RESEARCH.md):
```typescript
// import('3dmol') inside useEffect — NEVER at module level
// $3Dmol.createViewer(container, { backgroundColor: 'transparent' })
// viewer.addModel(xyzData, 'xyz')
// viewer.setStyle({}, { sphere: { radius: 0.3 }, stick: { radius: 0.15 } })
// viewer.addUnitCell(model, { box: { color: 'grey', linewidth: 1 } })
// viewer.zoomTo() / viewer.render()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install 3Dmol.js, create Mol3DViewer and StructureModal, add types and hook</name>
  <files>src/lib/types.ts, src/lib/api.ts, src/components/Mol3DViewer.tsx, src/components/StructureModal.tsx</files>
  <action>
    1. **Install 3Dmol.js:** `bun add 3dmol`

    2. **Add types to `src/lib/types.ts`:**
       ```typescript
       export interface StructureData {
         xyz_string: string;
         has_pbc: boolean;
       }

       export interface StructureResponse {
         data: StructureData;
       }
       ```

    3. **Add SWR hook to `src/lib/api.ts`:**
       - `useStructure(benchmarkSlug: string | null, model: string | null, filename: string | null)`: fetches `/api/v1/structures/${benchmarkSlug}/${model}/${filename}` when all params non-null. Returns `{ structureData, isLoading, error }`.

    4. **Create `src/components/Mol3DViewer.tsx`:**
       - `'use client'` component. This is the DYNAMIC IMPORT TARGET — never import directly.
       - Props: `xyzData: string`, `hasPbc: boolean`.
       - Use `useRef<HTMLDivElement>` for container and `useRef<any>` for viewer instance.
       - In `useEffect`, dynamically `import('3dmol')` then:
         - Create viewer with `backgroundColor: 'transparent'` (inherits from MUI Paper).
         - `viewer.addModel(xyzData, 'xyz')`.
         - `viewer.setStyle({}, { sphere: { radius: 0.3 }, stick: { radius: 0.15 } })` — ball-and-stick per user decision.
         - If `hasPbc`, call `viewer.addUnitCell(model, { box: { color: 'grey', linewidth: 1 } })` for wireframe unit cell box per user decision.
         - `viewer.zoomTo()` then `viewer.render()`.
       - Store viewer in ref. On cleanup, set ref to null (3Dmol.js handles its own WebGL cleanup).
       - Re-create viewer only when `xyzData` or `hasPbc` changes (dependency array).
       - Container: `width: '100%', height: '500px', position: 'relative'`.
       - CRITICAL: Never import `3dmol` at the top of this file — always inside `useEffect` or a function.

    5. **Create `src/components/StructureModal.tsx`:**
       - `'use client'` component.
       - Props: `open: boolean`, `onClose: () => void`, `benchmarkSlug: string`, `model: string`, `filename: string`.
       - Import Mol3DViewer via `dynamic(() => import('./Mol3DViewer'), { ssr: false })`.
       - Use `useStructure(benchmarkSlug, model, filename)` hook.
       - Render MUI Dialog (fullWidth, maxWidth 'md'):
         - DialogTitle: "Structure: {filename}" with close button.
         - DialogContent: Loading skeleton while fetching, error message on failure, Mol3DViewer when data loaded.
       - Pass `xyzData={structureData.xyz_string}` and `hasPbc={structureData.has_pbc}` to Mol3DViewer.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Mol3DViewer and StructureModal compile, 3dmol installed, types and hook defined, no SSR import of 3dmol</done>
</task>

<task type="auto">
  <name>Task 2: Add "View Structure" button to FigureDrawer</name>
  <files>src/components/FigureDrawer.tsx</files>
  <action>
    1. **Read `src/components/FigureDrawer.tsx`** before modifying.

    2. **Determine structure availability:** The figure drawer already receives `benchmarkSlug` and `filterModel`. To show the "View Structure" button, we need to know if the benchmark has xyz files. Options:
       - Add a new SWR hook `useStructureFiles(benchmarkSlug, model)` that calls a structure listing endpoint. BUT the backend doesn't have a structure listing endpoint.
       - Simpler approach: Add the button always and let the StructureModal handle 404 gracefully (show "No structure available" message). This avoids a new endpoint just for checking existence.
       - Better: Check if any of the figure slugs suggest structure data, OR just show the button for all benchmarks and let the modal handle missing data gracefully.

       **Decision:** Show "View Structure" button in the drawer header area. When clicked, open StructureModal. The modal's `useStructure` hook will return 404 for benchmarks without structures — the StructureModal should show a friendly "No structure data available for this benchmark/model" message on error. This is the simplest approach per user decision ("appears only for benchmarks with xyz files" — but detecting availability client-side without a list endpoint requires a new API; defer to showing the button universally and handling the 404 gracefully. The user can request a structure-availability endpoint later if needed).

       **Alternative (preferred if simple):** Add a `useStructureAvailability(benchmarkSlug: string | null)` hook that calls `GET /api/v1/structures/{slug}/available` — but this requires a new backend endpoint. Since Plan 01 is already built, add this endpoint inline in FigureDrawer: make a lightweight SWR fetch to the structure endpoint with a known model and check if it 404s. OR: Pass structure availability info from the parent.

       **Final decision:** Show the "View Structure" button ONLY when `filterModel` is set (cell-click, not header-click). The button needs a specific model + benchmark to fetch a structure. When model is null (all models view from header click), hide the button. For the filename, use a convention: fetch the first available structure file. Add a new minimal hook `useFirstStructureFile(benchmarkSlug, model)` that tries listing and picks the first xyz.

       **Simplest correct approach:** Add state `structureOpen: boolean` and `structureFile: string | null` to FigureDrawer. Show a "View Structure" button in the drawer header when `filterModel` is set. On click, open StructureModal with `benchmarkSlug`, `model=filterModel`, and `filename` set to a default like `{filterModel}.xyz` (common pattern in the data). The StructureModal handles 404 gracefully. This matches user decision without adding new backend work.

    3. **Implementation:**
       - Import StructureModal (lazy via `dynamic` is NOT needed — StructureModal itself lazy-loads Mol3DViewer).
       - Add state: `const [structureOpen, setStructureOpen] = useState(false)`.
       - In the drawer header area (next to close button), when `filterModel` is set, add an MUI Button (variant="outlined", size="small", startIcon=<ViewInArIcon/>) labeled "View Structure".
       - On click: `setStructureOpen(true)`.
       - Render `<StructureModal open={structureOpen} onClose={() => setStructureOpen(false)} benchmarkSlug={benchmarkSlug!} model={filterModel!} filename={filterModel + '.xyz'} />` when structureOpen is true.
       - StructureModal handles the case where no structure exists (404 from API → shows error message).
       - When drawer closes or benchmarkSlug/filterModel changes, reset structureOpen to false.
  </action>
  <verify>
    <automated>npx tsc --noEmit && bun run build 2>&1 | tail -5</automated>
  </verify>
  <done>"View Structure" button appears in figure drawer when a specific model is selected, clicking opens a modal with 3D structure (or error for benchmarks without structures), build succeeds</done>
</task>

</tasks>

<verification>
```bash
# TypeScript compiles
npx tsc --noEmit

# Build succeeds (3Dmol.js not in initial bundle)
bun run build

# 3dmol is installed
bun pm ls 2>/dev/null | grep 3dmol || cat node_modules/3dmol/package.json | head -3
```
</verification>

<success_criteria>
- 3Dmol.js installed and lazy-loaded (not in initial page bundle)
- Mol3DViewer renders ball-and-stick style with unit cell for pbc structures
- StructureModal shows loading state, 3D viewer, or error
- FigureDrawer shows "View Structure" button when filterModel is set
- Clicking the button opens the structure modal
- Works in dark and light mode (transparent background)
- TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-secondary-viewers/04-03-SUMMARY.md`
</output>
