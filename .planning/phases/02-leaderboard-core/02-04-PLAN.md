---
phase: 02-leaderboard-core
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - src/app/page.tsx
  - src/app/[category]/page.tsx
  - src/app/[category]/[benchmark]/page.tsx
  - src/app/[category]/layout.tsx
  - src/components/SummaryTable.tsx
autonomous: false
requirements: [FR-1.4, FR-5.3, NFR-1.1]

must_haves:
  truths:
    - "/ route shows Summary table with one Score column per category and an overall Score"
    - "/bulk_crystal route loads first benchmark in that category automatically"
    - "/bulk_crystal/elasticity route shows the elasticity benchmark table"
    - "Browser back/forward navigation works between categories and benchmarks"
    - "Direct URL navigation (deep link) loads the correct category and benchmark"
    - "Page loads in under 2 seconds on broadband"
    - "SWR caching prevents re-fetch when navigating back to a previously loaded benchmark"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Summary page with aggregated scores"
      contains: "SummaryTable"
    - path: "src/app/[category]/page.tsx"
      provides: "Category page redirecting to first benchmark"
      contains: "redirect"
    - path: "src/app/[category]/[benchmark]/page.tsx"
      provides: "Benchmark detail page with LeaderboardTable"
      contains: "LeaderboardTable"
    - path: "src/app/[category]/layout.tsx"
      provides: "Category layout with CategoryTabs and BenchmarkSubTabs"
      contains: "CategoryTabs"
    - path: "src/components/SummaryTable.tsx"
      provides: "DataGrid for summary view (Score per category + overall)"
      contains: "DataGrid"
  key_links:
    - from: "src/app/[category]/[benchmark]/page.tsx"
      to: "src/lib/api.ts"
      via: "useBenchmarkTable(slug) hook"
      pattern: "useBenchmarkTable"
    - from: "src/app/[category]/[benchmark]/page.tsx"
      to: "src/components/LeaderboardTable.tsx"
      via: "renders LeaderboardTable with fetched data"
      pattern: "<LeaderboardTable"
    - from: "src/app/page.tsx"
      to: "src/components/SummaryTable.tsx"
      via: "renders SummaryTable on / route"
      pattern: "<SummaryTable"
    - from: "src/app/[category]/layout.tsx"
      to: "src/components/CategoryTabs.tsx"
      via: "renders tab navigation in category layout"
      pattern: "<CategoryTabs"
---

<objective>
Wire App Router pages for summary, category, and benchmark routes; build SummaryTable component; connect all components into a working navigation flow with deep-linkable URLs.

Purpose: Completes the leaderboard by connecting all components into navigable pages. Researchers can browse the summary, navigate to any category/benchmark, and share deep links.
Output: Fully functional leaderboard with routing, summary table, and data loading.
</objective>

<execution_context>
@/Users/fzills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fzills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-leaderboard-core/02-CONTEXT.md
@.planning/phases/02-leaderboard-core/02-RESEARCH.md
@.planning/phases/02-leaderboard-core/02-01-SUMMARY.md
@.planning/phases/02-leaderboard-core/02-02-SUMMARY.md
@.planning/phases/02-leaderboard-core/02-03-SUMMARY.md

<interfaces>
From src/lib/api.ts (Plan 02):
```typescript
export function useCategories(): { categories: Category[], isLoading, error }
export function useBenchmarkTable(slug: string | null): { tableData: BenchmarkTableResponse, isLoading, error }
```

From src/components/LeaderboardTable.tsx (Plan 03):
```typescript
// Props: rows: MetricsRow[], meta: BenchmarkMeta
export function LeaderboardTable({ rows, meta }: LeaderboardTableProps): JSX.Element
```

From src/components/CategoryTabs.tsx (Plan 02):
```typescript
export function CategoryTabs({ categories, activeCategory }: Props): JSX.Element
```

From src/components/BenchmarkSubTabs.tsx (Plan 02):
```typescript
export function BenchmarkSubTabs({ benchmarks, activeBenchmark, categorySlug }: Props): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SummaryTable component and summary page</name>
  <files>src/components/SummaryTable.tsx, src/app/page.tsx</files>
  <action>
1. Create src/components/SummaryTable.tsx ('use client'):
   - Fetches all categories via useCategories() hook
   - For each category, fetches the first benchmark's table data to extract Score per model (or fetches a dedicated summary if available)
   - Actually: The summary approach is simpler — for each category, the Score column represents the category's aggregate score. Fetch each category's benchmarks and aggregate.
   - Simpler approach per Claude's discretion: Create a summary by iterating categories. For each category, take the first benchmark's Score values as representative (weighted average deferred to Phase 5 per ROADMAP). Show DataGrid with:
     - MLIP column (model display name)
     - One column per category (showing the average Score across that category's benchmarks)
     - Overall Score column (average of all category scores)
   - Use SWR for data fetching with deduplication
   - Apply viridis_r coloring to all Score columns (same as LeaderboardTable)
   - Missing scores show hatched pattern
   - Use formatSigFigs for number display

   IMPORTANT: The summary table may need multiple API calls (one per category's benchmarks). To keep initial load fast (NFR-1.1), fetch categories first, then lazily fetch benchmark data. Show a TableSkeleton while loading.

   Alternative simpler approach (preferred): Use the existing per-benchmark Score values. For each model, compute average Score across all benchmarks in a category. This requires fetching all benchmark tables — do this client-side with SWR parallel fetching. Cache means subsequent visits are instant.

2. Update src/app/page.tsx:
   - 'use client' component
   - Render CategoryTabs (with categories from useCategories, activeCategory="summary") at the top
   - Render SummaryTable below the tabs
   - Full viewport height layout: flex column, tabs at top, table fills remaining space
   - Wrap in a Box with `display: 'flex', flexDirection: 'column', height: '100vh'` minus AppBar height
  </action>
  <verify>
    <automated>cd /Users/fzills/tools/ml-peg-ui && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Summary page at / shows aggregated scores per category in a color-coded DataGrid; CategoryTabs visible with "Summary" active</done>
</task>

<task type="auto">
  <name>Task 2: Create App Router pages for category and benchmark routes</name>
  <files>src/app/[category]/layout.tsx, src/app/[category]/page.tsx, src/app/[category]/[benchmark]/page.tsx</files>
  <action>
1. Create src/app/[category]/layout.tsx ('use client'):
   - Accepts `params: { category: string }` and `children`
   - Fetches categories via useCategories()
   - Find current category from categories list using params.category slug
   - Renders:
     - CategoryTabs with categories and activeCategory=params.category
     - BenchmarkSubTabs with current category's benchmarks, activeBenchmark derived from URL, categorySlug=params.category
     - {children} below the tabs
   - Layout fills remaining viewport height below AppBar

2. Create src/app/[category]/page.tsx ('use client'):
   - Accepts `params: { category: string }`
   - Fetches categories via useCategories()
   - Finds the first benchmark in the matching category
   - Redirects to `/${category}/${firstBenchmark.slug}` using `router.replace()` (not hard redirect — keeps SPA navigation)
   - Shows TableSkeleton while loading categories
   - Shows error message if category not found

3. Create src/app/[category]/[benchmark]/page.tsx ('use client'):
   - Accepts `params: { category: string; benchmark: string }`
   - Fetches benchmark table via useBenchmarkTable(params.benchmark)
   - While loading: render TableSkeleton
   - On error: render error message with retry suggestion
   - On success: render `<LeaderboardTable rows={tableData.data} meta={tableData.meta} />`
   - The table fills remaining height below tabs (flex: 1)

URL structure (FR-5.3 deep links):
- `/` -> Summary page (handled by page.tsx above)
- `/bulk_crystal` -> Redirects to `/bulk_crystal/elasticity` (first benchmark)
- `/bulk_crystal/elasticity` -> Shows elasticity benchmark table
- Direct URL navigation works because App Router reads params from URL segments
  </action>
  <verify>
    <automated>cd /Users/fzills/tools/ml-peg-ui && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Category pages redirect to first benchmark; benchmark pages show LeaderboardTable with fetched data; deep links work; back/forward navigation preserves state</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete leaderboard flow</name>
  <files>none</files>
  <action>
Human verification checkpoint. Start the dev server and verify the complete leaderboard flow.

What was built:
- Summary table at / showing aggregated scores per category
- 16 category tabs + benchmark sub-tabs
- Color-coded DataGrid with viridis_r heatmap for each benchmark
- Dark/light mode toggle
- Sticky MLIP/Score columns on horizontal scroll
- GitHub model links
- Deep-linkable URLs

Steps to verify:
1. Run `bun run dev` and open http://localhost:3000
2. Verify Summary tab is active, showing a table with category Score columns
3. Click a category tab (e.g., "Bulk Crystal") — should show benchmark sub-tabs and redirect to first benchmark
4. Click a benchmark sub-tab — table should update with that benchmark's metrics
5. Verify cells are color-coded (yellow=good, purple=bad) with viridis_r gradient
6. Find a benchmark with missing values — verify gray hatched pattern appears
7. Toggle dark mode — verify no flash, colors remain readable, preference persists on reload
8. Scroll horizontally on a wide table (try GSCDB138 benchmarks) — MLIP and Score should stay pinned
9. Hover a column header — tooltip should show metric description
10. Navigate directly to a deep link URL like http://localhost:3000/bulk_crystal/elasticity — should load correctly
11. Use browser back/forward — should work naturally
12. Check that page load feels fast (< 2 seconds target)

Resume: Type "approved" or describe issues to fix.
  </action>
  <verify>User confirms visual and functional correctness</verify>
  <done>User approved the complete leaderboard flow — all navigation, color-coding, dark/light mode, and deep linking working correctly</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — all TypeScript compiles
- `uv run pytest tests/ -x -q` — all Python tests pass
- Manual: Full navigation flow works (summary -> category -> benchmark -> back)
- Manual: Deep links load correct content
- Manual: Dark/light mode works across all pages
- Manual: Performance feels fast (< 2s initial load)
</verification>

<success_criteria>
- Summary table shows aggregated category scores (FR-1.4)
- All routes deep-linkable (FR-5.3)
- Page loads under 2 seconds (NFR-1.1)
- Complete navigation flow works with back/forward
- User approves the visual result at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-leaderboard-core/02-04-SUMMARY.md`
</output>
