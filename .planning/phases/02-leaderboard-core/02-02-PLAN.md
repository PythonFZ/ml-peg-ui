---
phase: 02-leaderboard-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - src/app/layout.tsx
  - src/app/providers.tsx
  - src/components/AppHeader.tsx
  - src/components/CategoryTabs.tsx
  - src/components/BenchmarkSubTabs.tsx
  - src/lib/api.ts
autonomous: true
requirements: [FR-4.1, FR-4.2, FR-4.3, FR-1.3, FR-5.2, NFR-4.3]

must_haves:
  truths:
    - "MUI packages installed and working with Next.js 15 App Router"
    - "Dark/light mode toggle works without SSR flash"
    - "Theme preference persists in localStorage across page reloads"
    - "AppBar shows ML-PEG title, GitHub link icon, and theme toggle"
    - "16 category tabs are visible and scrollable"
    - "Category tabs and theme toggle have accessible aria-labels"
  artifacts:
    - path: "src/app/providers.tsx"
      provides: "MUI ThemeProvider with CSS Variables and CssBaseline"
      contains: "cssVariables"
    - path: "src/app/layout.tsx"
      provides: "Root layout with AppRouterCacheProvider and getInitColorSchemeScript"
      contains: "getInitColorSchemeScript"
    - path: "src/components/AppHeader.tsx"
      provides: "AppBar with title, GitHub icon, theme toggle"
      contains: "useColorScheme"
    - path: "src/components/CategoryTabs.tsx"
      provides: "Scrollable category tab bar synced to URL"
      contains: "variant.*scrollable"
    - path: "src/components/BenchmarkSubTabs.tsx"
      provides: "Sub-tab row for benchmarks within a category"
      contains: "Tabs"
    - path: "src/lib/api.ts"
      provides: "SWR-based fetch helpers for categories and benchmark tables"
      contains: "useSWR"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/providers.tsx"
      via: "import Providers"
      pattern: "import.*Providers"
    - from: "src/components/CategoryTabs.tsx"
      to: "src/lib/api.ts"
      via: "useCategories hook"
      pattern: "useCategories"
    - from: "src/components/AppHeader.tsx"
      to: "@mui/material/styles"
      via: "useColorScheme for theme toggle"
      pattern: "useColorScheme"
---

<objective>
Install MUI packages, wire dark/light mode with CSS Variables (no SSR flash), create the app shell (AppBar + category tabs + benchmark sub-tabs), and set up SWR data fetching.

Purpose: Establishes the visual shell and navigation that all subsequent plans build within. Dark/light mode and the tab navigation are the structural foundation.
Output: Working app shell with theme toggle, 16 scrollable category tabs, and SWR fetch hooks.
</objective>

<execution_context>
@/Users/fzills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fzills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-leaderboard-core/02-CONTEXT.md
@.planning/phases/02-leaderboard-core/02-RESEARCH.md

<interfaces>
From src/lib/types.ts (existing):
```typescript
export interface Category {
  slug: string;
  name: string;
  benchmarks: Array<{ slug: string; name: string }>;
}
```

API endpoints (from Phase 1):
- GET /api/v1/categories -> { data: Category[], meta: { count: number } }
- GET /api/v1/benchmarks/{slug}/table -> { data: MetricsRow[], meta: BenchmarkMeta }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install MUI packages and wire theme providers</name>
  <files>package.json, bun.lock, src/app/layout.tsx, src/app/providers.tsx</files>
  <action>
1. Install packages:
   ```bash
   bun add @mui/material @mui/x-data-grid @emotion/react @emotion/styled @mui/material-nextjs @mui/icons-material d3-scale-chromatic swr
   bun add -d @types/d3-scale-chromatic
   ```

2. Create src/app/providers.tsx ('use client' component):
   - `createTheme({ cssVariables: true, colorSchemes: { light: {}, dark: {} } })`
   - Wrap children in `<ThemeProvider theme={theme}><CssBaseline />{children}</ThemeProvider>`
   - Export `Providers` component

3. Update src/app/layout.tsx (Server Component):
   - Import `AppRouterCacheProvider` from `@mui/material-nextjs/v15-appRouter`
   - Import `getInitColorSchemeScript` from `@mui/material/styles`
   - Add `suppressHydrationWarning` to `<html>` tag
   - Render: `{getInitColorSchemeScript()}` before `<AppRouterCacheProvider><Providers>{children}</Providers></AppRouterCacheProvider>`
   - Keep existing metadata (title "ML-PEG Leaderboard")

This wires MUI CSS Variables dark/light mode with zero SSR flash. FR-4.2 (CSS Variables API) and FR-4.3 (localStorage persistence) are handled automatically by MUI's built-in mechanism.
  </action>
  <verify>
    <automated>cd /Users/fzills/tools/ml-peg-ui && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>MUI packages installed; providers.tsx and layout.tsx wired; tsc compiles; dark/light mode toggleable without SSR flash</done>
</task>

<task type="auto">
  <name>Task 2: Create AppHeader, CategoryTabs, BenchmarkSubTabs, and SWR fetch hooks</name>
  <files>src/components/AppHeader.tsx, src/components/CategoryTabs.tsx, src/components/BenchmarkSubTabs.tsx, src/lib/api.ts</files>
  <action>
1. Create src/lib/api.ts — SWR fetch helpers:
   - `fetcher` function: `(url: string) => fetch(url).then(r => r.json())`
   - `useCategories()` hook: `useSWR<ApiEnvelope<Category[]>>('/api/v1/categories', fetcher, { revalidateOnFocus: false })` — returns `{ categories, isLoading, error }`
   - `useBenchmarkTable(slug: string | null)` hook: `useSWR(slug ? '/api/v1/benchmarks/${slug}/table' : null, fetcher)` — conditional fetch, returns `{ tableData, isLoading, error }`
   - Both hooks use SWR deduplication and stale-while-revalidate for performance (NFR-1.1)

2. Create src/components/AppHeader.tsx ('use client'):
   - MUI AppBar with position="sticky"
   - Left: Typography "ML-PEG" as title
   - Right: IconButton with GitHub icon linking to https://github.com/ddmms/ml-peg (target="_blank", rel="noopener"), aria-label="GitHub repository"
   - Right: Theme toggle IconButton using `useColorScheme()` — Brightness4 (to dark) / Brightness7 (to light), aria-label="toggle dark/light mode" (NFR-4.3)
   - Handle `mode === undefined` on server by defaulting to light icon

3. Create src/components/CategoryTabs.tsx ('use client'):
   - Accept `categories: Category[]`, `activeCategory: string` props
   - MUI Tabs with `variant="scrollable"`, `scrollButtons="auto"`
   - First Tab: label="Summary", value="summary"
   - Remaining Tabs: one per category, value=category.slug, label=category.name
   - `value` derived from `activeCategory` prop (empty string or missing = "summary")
   - onChange: `router.push(value === 'summary' ? '/' : '/${value}')`
   - Use `useRouter` from `next/navigation` for navigation

4. Create src/components/BenchmarkSubTabs.tsx ('use client'):
   - Accept `benchmarks: Array<{ slug: string; name: string }>`, `activeBenchmark: string`, `categorySlug: string` props
   - MUI Tabs with `variant="scrollable"`, `scrollButtons="auto"`, smaller size
   - One Tab per benchmark: value=benchmark.slug, label=benchmark.name
   - onChange: `router.push('/${categorySlug}/${value}')`
   - If only one benchmark, still render the single tab (for consistency)
  </action>
  <verify>
    <automated>cd /Users/fzills/tools/ml-peg-ui && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>AppHeader renders with title, GitHub link, and working theme toggle; CategoryTabs renders 16+ scrollable tabs synced to URL; BenchmarkSubTabs renders benchmark options; SWR hooks fetch from API; all compile without errors</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — TypeScript compiles without errors
- `bun run dev` — app starts, shows AppHeader with theme toggle
- Theme toggle switches between dark and light; page reload preserves choice
- Category tabs scroll horizontally; clicking a tab changes URL
</verification>

<success_criteria>
- MUI CSS Variables dark/light mode works without SSR flash (FR-4.1, FR-4.2, FR-4.3)
- AppBar visible with title, GitHub link, theme toggle (FR-5.1 partial)
- 16 category tabs + Summary tab visible and scrollable (FR-1.3, FR-5.2)
- SWR hooks ready for data fetching (NFR-1.1)
- All interactive elements have aria-labels (NFR-4.3)
</success_criteria>

<output>
After completion, create `.planning/phases/02-leaderboard-core/02-02-SUMMARY.md`
</output>
